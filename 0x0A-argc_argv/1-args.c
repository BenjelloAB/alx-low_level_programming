#include <stdio.h>

/**
 * main - returns the number of arguments passed into it
 * @argv: the list of arguments passed in
 * @argc: the number of arguments passed in
 * Return: int
 */
int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);

	return (0);
}
